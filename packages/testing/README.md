<!-- SPDX-License-Identifier: Apache-2.0 -->
# `sayfirst` port conformance kit

This distribution publishes reusable contract suites for the policy authority,
policy projection, and append-only decision store ports. A provider is a
provider only when its adapter passes the corresponding suite.
